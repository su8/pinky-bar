AM_CFLAGS = #amCF# -I/usr/local/include $(LIBNL_CF) $(PERL_CF) $(PYTHON_CF)

bin_PROGRAMS = pinkybar

EXTRA_PROGRAMS   = ncurses
ncurses_SOURCES  = ncurses.c
ncurses_LDADD    = -lncurses

pinkybar_LDADD = '${src_libs_to_add[@]}' #bsdLibs#
pinkybar_SOURCES = #sources#
