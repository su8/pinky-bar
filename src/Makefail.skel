AM_CFLAGS = {amCF} \
  -I/usr/local/include $(LIBNL_CF) $(PERL_CF) $(PYTHON_CF)

bin_PROGRAMS = pinkybar

EXTRA_PROGRAMS   = ncurses
ncurses_SOURCES  = ncurses.c
ncurses_LDADD    = -lncurses

pinkybar_LDADD = $(X_LIBS) $(ALSA_LIBS) $(MPD_LIBS) $(PCI_LIBS) $(DVD_LIBS) $(SENSORS_LIBS) $(CURL_LIBS) $(LIBNL_LZ) $(PERL_LZ) $(PYTHON_LZ) 
pinkybar_SOURCES = {srcFiles}
